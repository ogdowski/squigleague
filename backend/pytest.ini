[pytest]
# Use /tmp for cache to avoid permission issues in Docker
cache_dir = /tmp/.pytest_cache

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = -v --tb=short

# Async mode for pytest-asyncio
asyncio_mode = strict
