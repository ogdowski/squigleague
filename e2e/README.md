# End-to-End Tests Status

- Playwright assets have been removed per Commandment 26 (Selenium only).
- The active, compliant suite lives in `tests/e2e/selenium` (pytest + Selenium).
- Remaining HTML artifacts (e.g., login-page.html) are reference fixtures only.